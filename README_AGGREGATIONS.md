<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

## withLeadingDollar

### Parameters

*   `string` **[string][1]**&#x20;

Returns **[string][1]**&#x20;

## reduce

### Parameters

*   `array` **[Array][2]<[string][1]>**&#x20;
*   `value` **[string][1]**&#x20;

## EQUALS

### Parameters

*   `field` &#x20;
*   `target` &#x20;

### Properties

*   `$match` **[object][3]**&#x20;

## EQUALS

### Parameters

*   `field` **[string][1]** the name of the field
*   `target` **[string][1]** the target value

Returns **Match**&#x20;

## EQUALS\_ALL

### Parameters

*   `object` **[object][3]** an object containing they key value pairs to query for
*   `options` **[object][3]?** options (optional, default `{}`)

    *   `options.prefix` **[string][1]** a prefix to add to all keys

Returns **Match**&#x20;

## EQUALS\_ANY\_OF

### Parameters

*   `field` **[string][1]** the name of the field
*   `targets` **[Array][2]** the target value

Returns **Match**&#x20;

## EQUALS\_WILDCARD

Returns **Match**&#x20;

## LESS\_THAN

### Parameters

*   `field` **[string][1]** the name of the field
*   `target` **[string][1]** the target value

Returns **Match**&#x20;

## LESS\_THAN\_OR\_EQUAL

### Parameters

*   `field` **[string][1]** the name of the field
*   `target` **[string][1]** the target value

Returns **Match**&#x20;

## GREATER\_THAN

### Parameters

*   `field` **[string][1]** the name of the field
*   `target` **[string][1]** the target value

Returns **Match**&#x20;

## GREATER\_THAN\_OR\_EQUAL

### Parameters

*   `field` **[string][1]** the name of the field
*   `target` **[string][1]** the target value

Returns **Match**&#x20;

## LIMIT

### Parameters

*   `amount` &#x20;

### Properties

*   `$limit` **[object][3]**&#x20;

## LIMIT

### Parameters

*   `amount` **[number][4]** the amount of documents to return

Returns **Limit**&#x20;

## PROJECT

### Parameters

*   `projection` &#x20;

### Properties

*   `$limit` **[object][3]**&#x20;

## PROJECT

### Parameters

*   `projection` **[object][3]** the projection object

Returns **Projection**&#x20;

## SORT\_BY\_ASC

### Parameters

*   `field` **...any**&#x20;

### Properties

*   `$sort` **[object][3]**&#x20;

## SORT\_BY\_ASC

### Parameters

*   `field` **...[string][1]**&#x20;

Returns **Sort**&#x20;

## SORT\_BY\_DESC

### Parameters

*   `field` **...[string][1]**&#x20;

Returns **Sort**&#x20;

## SORT\_BY\_COUNT

### Parameters

*   `field` &#x20;

### Properties

*   `$sortByCount` **[object][3]**&#x20;

## SORT\_BY\_COUNT

### Parameters

*   `field` **[string][1]**&#x20;

Returns **SortByCount**&#x20;

## UNWIND

### Parameters

*   `field` &#x20;

### Properties

*   `$unwind` **[object][3]**&#x20;

## UNWIND

### Parameters

*   `field` **[string][1]**&#x20;

Returns **Unwind**&#x20;

## COUNT

### Properties

*   `$count` **[object][3]**&#x20;

## COUNT

Returns **Count**&#x20;

## LOOKUP

### Parameters

*   `$0` **[Object][3]**&#x20;

    *   `$0.from` &#x20;
    *   `$0.as` &#x20;
    *   `$0.localField` &#x20;
    *   `$0.foreignField`   (optional, default `'id'`)
    *   `$0.pipeline`   (optional, default `[]`)

### Properties

*   `$lookup` **[object][3]**&#x20;

## LOOKUP

### Parameters

*   `$0` **[Object][3]**&#x20;

    *   `$0.from` &#x20;
    *   `$0.as` &#x20;
    *   `$0.localField` &#x20;
    *   `$0.foreignField`   (optional, default `'id'`)
    *   `$0.pipeline`   (optional, default `[]`)

### Properties

*   `from` **[string][1]** the collection to lookup from
*   `as` **[string][1]** name of the merged field
*   `localField` **[string][1]** local field name
*   `foreignField` **[string][1]** field name of the `from` collection
*   `pipeline` **[Array][2]** pipeline for  of the lookup query

## LOOKUP

### Parameters

*   `options` **LookupOptions**&#x20;

    *   `options.from` &#x20;
    *   `options.as` &#x20;
    *   `options.localField` &#x20;
    *   `options.foreignField`   (optional, default `'id'`)
    *   `options.pipeline`   (optional, default `[]`)

Returns **Lookup**&#x20;

## REDUCE

### Parameters

*   `$0` **[Object][3]**&#x20;

    *   `$0.input` &#x20;
    *   `$0.initialValue`   (optional, default `{}`)
    *   `$0.inExpression` &#x20;

### Properties

*   `$reduce` **[object][3]**&#x20;

## REDUCE

### Parameters

*   `$0` **[Object][3]**&#x20;

    *   `$0.input` &#x20;
    *   `$0.initialValue`   (optional, default `{}`)
    *   `$0.inExpression` &#x20;

### Properties

*   `input` **[string][1]** any expression resolving to an array
*   `initialValue` **any** the initial value used for reduction
*   `inExpression` **[object][3]** any expression applied to each element of the array

## REDUCE

### Parameters

*   `options` **ReduceOptions**&#x20;

    *   `options.input` &#x20;
    *   `options.initialValue`   (optional, default `{}`)
    *   `options.inExpression` &#x20;

Returns **Reduce**&#x20;

## APPEND\_OBJECTS

### Parameters

*   `expressions` **...any**&#x20;

### Properties

*   `mergeObjects` **[object][3]**&#x20;

## APPEND\_OBJECTS

Returns a $mergeObjects expression that appends the result of the given expressions to $$value

### Parameters

*   `expressions` **...any** a list of expressions that evaluate to an object

Returns **AppendObjects**&#x20;

## LET

### Parameters

*   `$0` **[Object][3]**&#x20;

    *   `$0.vars` &#x20;
    *   `$0.inExpression` &#x20;

### Properties

*   `$let` **[object][3]**&#x20;

## LET

### Parameters

*   `$0` **[Object][3]**&#x20;

    *   `$0.vars` &#x20;
    *   `$0.inExpression` &#x20;

### Properties

*   `vars` **[object][3]** an object defining additional variables for the expression
*   `inExpression` **[object][3]** any expression

## LET

### Parameters

*   `options` **LetOptions**&#x20;

    *   `options.vars` &#x20;
    *   `options.inExpression` &#x20;

Returns **Let**&#x20;

## TO\_OBJECT

### Parameters

*   `$0` **[Object][3]**&#x20;

    *   `$0.key`   (optional, default `'$$this'`)
    *   `$0.value` &#x20;

### Properties

*   `arrayToObject` **[object][3]**&#x20;

## TO\_OBJECT

### Parameters

*   `$0` **[Object][3]**&#x20;

    *   `$0.key`   (optional, default `'$$this'`)
    *   `$0.value` &#x20;

### Properties

*   `key` **[string][1]** the object key
*   `expression` **[object][3]** any expression

## TO\_OBJECT

### Parameters

*   `options` **ToObjectOptions**&#x20;

    *   `options.key`   (optional, default `'$$this'`)
    *   `options.value` &#x20;

Returns **ToObject**&#x20;

## CONCAT\_STRINGS

### Parameters

*   `strings` **...any**&#x20;

### Properties

*   `$concat` **[object][3]**&#x20;

## CONCAT\_STRINGS

### Parameters

*   `strings` **...strings** strings to concat

Returns **Concat**&#x20;

## JOIN\_STRINGS

### Parameters

*   `separator` **[string][1]** separator to be used for joining given strings
*   `strings` **...[string][1]** strings to concat

Returns **Concat**&#x20;

## CONCAT\_ARRAYS

### Properties

*   `$concatArrays` **[Array][2]<[string][1]>**&#x20;

## CONCAT\_ARRAYS

Returns **ConcatArray**&#x20;

## ELEMENT\_AT

### Parameters

*   `arrayName` &#x20;
*   `index` &#x20;

### Properties

*   `$arrayElemAt` **[Array][2]<[string][1]>**&#x20;

## ELEMENT\_AT

### Parameters

*   `arrayName` **[string][1]** name of the array
*   `index` **[number][4]**&#x20;

Returns **ArrayElementAt**&#x20;

## AS\_ROOT

### Parameters

*   `fieldName` &#x20;

### Properties

*   `$replaceRoot` **[object][3]**&#x20;

    *   `$replaceRoot.newRoot` **[string][1]**&#x20;

## AS\_ROOT

### Parameters

*   `fieldName` **[string][1]** the field to return as root

Returns **ReplaceRoot**&#x20;

## EXISTS

### Parameters

*   `fieldName` **[string][1]** the target fieldname

Returns **Match**&#x20;

## TO\_LONG

### Parameters

*   `fieldname` &#x20;

### Properties

*   `$set` **[object][3]**&#x20;

## TO\_LONG

### Parameters

*   `fieldname` **[string][1]** the target fieldname

Returns **[Set][5]**&#x20;

## DATE\_TRUNC

### Parameters

*   `fieldname` &#x20;
*   `$1` **[Object][3]**&#x20;

    *   `$1.unit` &#x20;
    *   `$1.timezone`   (optional, default `'Europe/Berlin'`)
    *   `$1.binSize`   (optional, default `1`)
    *   `$1.startOfWeek`   (optional, default `'monday'`)

### Properties

*   `$dateTrunc` **[object][3]**&#x20;

## DATE\_TRUNC

*   **See**: [https://www.mongodb.com/docs/manual/reference/operator//dateTrunc/][6]

### Parameters

*   `fieldname` **[string][1]**&#x20;
*   `options` **[object][3]**&#x20;

    *   `options.unit` **[string][1]** e.g. day, month, quarter, year.
    *   `options.timezone` **[string][1]**  (optional, default `Europe/Berlin`)
    *   `options.binSize` **[number][4]** specifies the amount units (optional, default `1`)
    *   `options.startOfWeek` **[string][1]**  (optional, default `monday`)

Returns **DateTrunc**&#x20;

## INDEX\_STATS

### Properties

*   `$indexStats` **[object][3]**&#x20;

## INDEX\_STATS

Returns **IndexStats**&#x20;

[1]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String

[2]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array

[3]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object

[4]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Number

[5]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Set

[6]: https://www.mongodb.com/docs/manual/reference/operator//dateTrunc/
